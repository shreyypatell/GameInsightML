[build]
  base = "static"
  publish = "."
  command = "echo 'static assets only, backend deployed separately'"

[[redirects]]
  from = "/*"
  to = "https://gameinsight-ml.onrender.com/:splat"
  status = 200
  force = true